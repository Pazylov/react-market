{product.images.map(image => (
							<li
								onClick={() => setSelectedImage(image)}
								className={`${styles.imagesItem} ${
									selectedImage === image ? styles.active : ''
								}`}
								key={image}
							>
								<img src={`${url}${image}`} alt={image} />
							</li>
						))}



						> .imagesList {
			@apply w-full h-full grid grid-cols-4 gap-2  flex-wrap;

			> .imagesItem {
				@apply w-full h-full shadow-card cursor-pointer overflow-hidden;

				&.active {
					@apply border border-solid border-whiskySour;
				}

				> img {
					@apply transition-transform ease-linear;

					&:hover {
						@apply scale-125;
					}
				}
			}
		}